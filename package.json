{
  "name": "career-agent-pro-root",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "CI=false npm run build --prefix frontend",
    "start": "node backend/index.js"
  }
}
